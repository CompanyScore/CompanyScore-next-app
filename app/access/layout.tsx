import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'CompanyScore | Доступ',
  description: 'Generated by create next app',
};

export default async function AccessLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
